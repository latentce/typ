//! Persistence for `typ`: sessions, input events, and the derived caches.
//!
//! Nothing is stored yet; this crate exists so the dependency graph is fixed
//! from the start (`typ-rs-store` depends on `typ-rs-core`, never the reverse).
